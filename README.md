# 🎓 Student Management System

This is a Java-based desktop application for managing student data, using MySQL for storage and JDBC for database connectivity.

## 🛠️ Technologies Used
- **Language:** Java (Core)
- **Database:** MySQL (XAMPP)
- **Connection:** JDBC (MySQL Connector/J)
- **IDE:** VS Code

## 🚀 How to Run
1. Start MySQL in XAMPP.
2. Create a database named `student_db`.
3. Add the JDBC Connector `.jar` file to your project libraries.
4. Run `Main.java`.
